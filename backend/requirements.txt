fastapi>=0.136
uvicorn[standard]>=0.34
sqlalchemy>=2.0
alembic>=1.13
pydantic>=2.10
pydantic[email]>=2.10
pydantic-settings>=2.6
httpx>=0.27
pytest>=8.0
pyjwt>=2.8
bcrypt>=4.1
python-multipart>=0.0.9
anthropic>=0.40
# Postgres driver — required so DATABASE_URL=postgresql+psycopg://… works in any
# real (persistent) deployment. SQLite (dev default) ignores it.
psycopg[binary]>=3.1
# Intermittent-demand forecasting engine (Croston/SBA/TSB/ADIDA/IMAPA + conformal
# prediction intervals). Apache-2.0. Used ONLY via app/services/forecasting_sf.py
# and gated behind settings.forecast_engine (default "builtin"), so it is inert
# until explicitly enabled. Pulls numba (compiled) + pandas — see THIRD_PARTY_LICENSES.md.
statsforecast>=2.0,<3.0
