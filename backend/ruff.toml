# Ruff config. Pyflakes (F) + pycodestyle errors (E) + isort (I), with a
# generous line length to suit the descriptive style in this codebase.
line-length = 120

[lint]
select = ["E", "F", "I"]
ignore = ["E501"]  # long lines are fine; docstrings and dict literals run wide
